name: Test action
on: ["push", "pull_request"]
jobs:
    main:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Run tests
              run: echo "Tests passed!"